#include <stdio.h>
#include <math.h>
#include"NeuralNetwork.h"
/*
float sigmoid(float x)
{
	return 1. / (1. + exp(x));
}

float dSigmoid(float x)
{
	float sig = sigmoid(x);
	return sig * (1. - sig);
}
*/
